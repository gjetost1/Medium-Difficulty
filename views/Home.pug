extends layout

append head
    style
        include ../public/stylesheets/storycard.css

include utils.pug

block content
    div(class='storyCard_container')
        each story in stories
            +storyCard(story)
